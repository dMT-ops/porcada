// Arquivo de Configuração de APIs (config.js)
// COPIE este arquivo como "config.js" e preencha com suas chaves reais.
// O config.js está no .gitignore e NUNCA deve ser commitado.
window.PORQUIM_CONFIG = {
    // Insira sua chave do Google Gemini aqui (Deixe entre as aspas)
    GEMINI_API_KEY: 'SUA_CHAVE_GEMINI_AQUI',

    // Insira sua chave da Brapi (Ações da B3) aqui (Deixe entre as aspas)
    BRAPI_API_KEY: 'SUA_CHAVE_BRAPI_AQUI'
};
